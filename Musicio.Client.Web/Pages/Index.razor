@page "/"
@using Musicio.Client.User
@inject ISessionService UserSessionService

@code {
    protected override async Task OnInitializedAsync()
    {
        string token = await UserSessionService.GetJwtCookie();
    }
}